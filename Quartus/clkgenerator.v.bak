module clkgenerator(clk);

    reg clk_sig;
    output clk = clk_sig;

    initial 
        clk_sig = 0;

    initial 
        begin
            forever 
                #20 clk_sig=~clk_sig;//100MHz
        end

endmodule